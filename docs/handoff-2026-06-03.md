# 더이음 전자결재 앱 인수인계 문서

최종 정리 시점: 2026-06-29, Asia/Seoul
작업 경로: `/Users/kyh/theieum`
현재 브랜치: `main`
GitHub 원격 저장소: `git@github.com:5240KYH/Theieum-app.git`
원격 기본 브랜치: `main`

## 최신 업데이트: Task 28 UI/UX 보안 개선 및 병렬 에이전트 운영

Task 28 문서:

```text
docs/handoffs/2026-06-29-task-28-ui-security-improvements.md
```

핵심 내용:

- 사용자 편의 UI/UX와 보안 개선을 병렬 AI 에이전트 팀 방식으로 나누어 진행했다.
- `MANAGER`가 관리자 전체 신청서 목록에서 신청서 상세로 이동할 수 있도록 route 권한을 맞췄다.
- 서버 역할 판정은 `RoleAccess`로 통합하고 `MANGER` alias, trim, 대소문자 정규화를 한 곳에서 처리한다.
- 첨부 파일 read/delete는 storage root 밖 경로와 root 밖 symlink target을 차단한다.
- 신청서 제출은 필수 항목별 안내와 `신청서 제출 확인` 모달을 거친다.
- 결재 승인은 `승인 확인` 모달을 거치고, 승인 실패 오류는 모달 내부에 표시한다.
- 관리자 비밀번호 변경에는 확인 입력을 추가했고, 완전 삭제와 월별 첨부 ZIP 다운로드는 별도 확인 절차를 둔다.
- 병렬 에이전트 방식은 UI/보안 작업 분리에 효과가 있었지만, backend 통합 테스트는 같은 `postgres-test` DB를 공유하므로 동시 실행 경합을 피해야 한다.

검증 진행:

```bash
cd frontend && npm run test -- AdminApplicationsPage.test.tsx ApplicationForm.test.tsx ApprovalsInboxPage.test.tsx AdminReferencePage.test.tsx
cd frontend && npm run test
cd frontend && npm run build
cd backend && TMPDIR=/private/tmp GRADLE_USER_HOME=/Users/kyh/theieum/.gradle-home JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./gradlew test --tests com.theieum.approval.attachment.LocalFileStorageTest
cd backend && TMPDIR=/private/tmp GRADLE_USER_HOME=/Users/kyh/theieum/.gradle-home JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./gradlew test --max-workers=1
docker compose config
git diff --check
```

실제 결과:

- frontend targeted 테스트 42개 통과
- frontend 전체 테스트 85개 통과
- frontend build 통과
- backend `LocalFileStorageTest` 5개 통과
- backend 전체 테스트는 단독 실행 조건인 `--max-workers=1`에서 통과
- `docker compose config` 통과
- `git diff --check` 통과

## 이전 최신 업데이트: Task 27 신청서 목록 상세 이동 및 조회 필터/관리 UI 개선

Task 27 문서:

```text
docs/handoffs/2026-06-11-task-27-application-list-detail-filters.md
```

핵심 내용:

- `내 신청서` 상세 화면에 `내 신청서로 돌아가기` 버튼을 추가했다.
- `전체 신청서` 목록에 상세 버튼을 추가하고, 상세 화면에서 `전체 신청서로 돌아가기`를 제공한다.
- `전체 신청서` 상세 조회 권한을 관리자 메뉴 접근 정책과 맞추기 위해 `MANAGER`도 읽을 수 있게 했다.
- `내 신청서`, `전체 신청서` 목록에 영수증 월 From/To 필터와 `전체기간` 초기화 버튼을 추가했다.
- 영수증 월 입력은 `YYYY-MM` 텍스트 마스크로 처리해 `20260699` 입력도 `2026-06`까지만 반영되도록 했다.
- `전체 신청서` 목록에 `신청자` 텍스트 검색을 추가해 이름 포함 검색을 지원한다.
- 관리자 신청서 목록 API와 화면에 `신청일`, `영수증 일자`, `금액` 정보를 추가했다.
- 신청서 목록 검색조건 UI를 패널 형태로 정돈했다.
- `결재함` 화면에 `접수기간 From/To` 월 검색과 `전체기간` 초기화 버튼을 추가했다.
- `캘린더`는 업무 메뉴에서 관리 메뉴(`/admin/calendar`)로 이동했고, `ADMIN`, `MANAGER`만 관리 화면에 접근한다.
- 기존 `/calendar` 주소는 대시보드로 이동하며, 일반 사용자는 대시보드의 공용 캘린더 위젯으로 일정을 조회한다.
- 캘린더 일정 등록/수정 날짜 입력은 텍스트 입력과 날짜 선택 input을 나란히 제공하며 `20260610 -> 2026-06-10`으로 자동 포맷된다.
- `알림 로그`는 제목/본문, 수신자명, 사용처/신청상태, 발송상태/채널/읽음, 생성/발송시각을 표시하도록 개선했다.
- 후속 정리로 `내 신청서`, `결재함`, `전체 신청서`의 상단 검색조건을 `SearchConditionPanel` 공통 컴포넌트로 통일했다.
- `AGENTS.md`에 목록 검색조건 UI는 공통 컴포넌트를 우선 사용하도록 프로젝트 규칙을 추가했다.
- 캘린더 날짜 직접 선택 컨트롤은 `시작일 일자 직접 선택`, `종료일 일자 직접 선택` `type="date"` 입력으로 명확히 표시한다.
- 알림 로그는 수신자 이름만 표시하고, 신청서 정보는 `사용처`/`신청상태`, 알림 정보는 `발송상태`/`채널`/`읽음`으로 분리했다.
- 알림 이벤트의 `CREATED` 상태는 관리자 화면에서 `생성` 대신 `발송 전`으로 표시한다.

검증 진행:

```bash
cd frontend && npm run test -- AdminApplicationsPage.test.tsx ApprovalsInboxPage.test.tsx CalendarPage.test.tsx AppLayout.test.tsx
cd frontend && npm run test -- MyApplicationsPage.test.tsx ApprovalsInboxPage.test.tsx AdminApplicationsPage.test.tsx CalendarPage.test.tsx
cd frontend && npm run test
cd frontend && npm run build
cd backend && TMPDIR=/private/tmp GRADLE_USER_HOME=/Users/kyh/theieum/.gradle-home JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./gradlew test --tests com.theieum.approval.api.ApiAuthorizationTest
docker compose up --build -d postgres backend frontend
docker compose ps
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000
```

실제 결과:

- frontend targeted 테스트 24개 통과
- frontend 후속 targeted 테스트 17개 통과
- frontend 전체 테스트 79개 통과
- frontend build 통과
- backend `ApiAuthorizationTest` 통과
- Docker frontend/backend/postgres 실행 중
- `http://localhost:3000`: HTTP 200 확인
- 인앱 브라우저에서 `admin / password` 로그인 후 `/admin/applications` 확인
  - 전체 신청서 목록에 상세/영수증 월 From~To/신청자 텍스트 검색 표시
  - 상세 진입 후 `전체 신청서로 돌아가기`로 목록 복귀 확인
  - 영수증 월과 신청자 검색 적용 시 대상 행만 표시되는 것 확인
  - `/admin/applications`에서 `영수증 월 From` `20260699 -> 2026-06`, `영수증 월 To` `202606 -> 2026-06`, `신청자` `채` 검색 확인
  - `/admin/calendar`가 관리 메뉴에 표시되고 기존 `/calendar`는 `/dashboard`로 redirect 확인
  - 캘린더 일정 등록 모달에서 `시작일` `20260610 -> 2026-06-10`, `종료일 캘린더 선택` `2026-06-11 -> 종료일 2026-06-11` 확인
  - `/approvals`에서 `접수기간 From/To` 월 마스크와 `전체기간` 초기화 확인
  - `/admin/notifications`에서 알림 내용, 수신자, 사용처, 신청상태, 발송상태, 채널, 읽음, 처리 시각 중심의 로그 표시 확인

## 이전 최신 업데이트: Task 26 신청서 입력 포맷 개선

Task 26 문서:

```text
docs/handoffs/2026-06-09-task-26-application-input-formatting.md
```

핵심 내용:

- 신청서 작성/수정 화면의 `영수증 일자` 입력을 `2026. 06. 08.` 형식으로 표시한다.
- 사용자가 `20260608`처럼 숫자만 입력해도 자동으로 `YYYY. MM. DD.` 형태로 정리한다.
- 저장/제출 API payload에는 기존 계약을 유지해 `receiptDate: "2026-06-08"`로 전송한다.
- `금액` 입력은 `100000` 입력 시 화면에 `100,000`처럼 천 단위 쉼표를 표시한다.
- 저장/제출 API payload에는 기존 계약을 유지해 `amount: 100000` 숫자로 전송한다.

검증 진행:

```bash
cd frontend && npm run test -- ApplicationForm.test.tsx
cd frontend && npm run test
cd frontend && npm run build
git diff --check
docker compose up --build -d frontend
docker compose ps
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000
```

실제 결과:

- frontend `ApplicationForm.test.tsx` 11개 통과
- frontend 전체 테스트 72개 통과
- frontend build 통과
- `git diff --check` 통과
- Docker frontend/backend/postgres 실행 중
- `http://localhost:3000` HTTP 200 확인
- headless 브라우저에서 `employee01 / password` 로그인 후 `/applications/new` 입력 포맷 확인
  - `20260608` -> `2026. 06. 08.`
  - `100000` -> `100,000`

## 이전 최신 업데이트: Task 25 조직장 기반 결재선

Task 25 문서:

```text
docs/handoffs/2026-06-08-task-25-organization-leader-approval.md
```

핵심 내용:

- `organizations.leader_user_id`를 추가하는 V6 Flyway 마이그레이션을 작성했다.
- `application_approval_steps`에 결재 당시 조직/직위 스냅샷을 저장하는 V7 Flyway 마이그레이션을 작성했다.
- 관리자 조직 API와 화면에서 조직장을 조회/지정할 수 있게 했다.
- 조직장은 해당 조직의 활성 소속자만 지정할 수 있다.
- 결재선 단계 타입에 `ORG_LEADER`를 추가했다.
- `ORG_LEADER`는 기존 조직범위(`APPLICANT_ORG`, `PARENT_ORG`, `ROOT_ORG`)를 그대로 사용해 팀/부 공통 결재선을 만든다.
- 팀 기준 `신청 조직 조직장 -> 상위 조직 조직장`, 부 기준 `신청 조직 조직장 -> 상위 조직 조직장` 흐름이 같은 설정으로 동작한다.
- 조직장 결재자 직위는 `user_organizations.position_id` 기준으로 표시/스냅샷에 반영된다.
- 제출된 결재 단계는 조직/직위 스냅샷을 보관하고, 신청서 상세 결재 진행 상태에 `조직명 이름 직위명`으로 표시한다.
- 조직장과 신청자가 같으면 Task 24 자동승인 로직을 그대로 사용한다.
- 관리자 결재선 화면에 `조직장` 단계 유형을 추가했고, 해당 유형은 조직범위만 입력한다.

검증 진행:

```bash
docker run --rm --network host -v /Users/kyh/theieum:/workspace -w /workspace/backend eclipse-temurin:21 ./gradlew test --tests com.theieum.approval.common.DatabaseMigrationTest --tests com.theieum.approval.approval.ApprovalLineResolverTest --tests com.theieum.approval.api.ApiAuthorizationTest --tests com.theieum.approval.admin.AdminHardDeleteServiceTest
cd frontend && npm run test -- AdminReferencePage.test.tsx
cd frontend && npm run test -- ApplicationDetailPage.test.tsx ApplicationForm.test.tsx AdminApplicationsPage.test.tsx
docker run --rm --network host -v /Users/kyh/theieum:/workspace -w /workspace/backend eclipse-temurin:21 ./gradlew test
cd frontend && npm run test
cd frontend && npm run build
git diff --check
docker compose config
docker compose --env-file .env.staging.example -f docker-compose.staging.yml config
docker compose up --build -d postgres backend frontend
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000
```

실제 결과:

- backend 조직장 targeted 테스트 통과
- frontend `AdminReferencePage.test.tsx` 15개 통과
- frontend 신청서 상세/작성/관리자 신청서 targeted 테스트 24개 통과
- backend 전체 테스트 119개 통과
- frontend 전체 테스트 71개 통과
- frontend build 통과
- `git diff --check` 통과
- local/staging compose config 통과
- Docker backend/frontend/postgres 실행 중
- Flyway v7 적용 확인
- `http://localhost:3000` HTTP 200 확인
- headless 브라우저에서 로그인 화면 렌더링 확인

## 이전 최신 업데이트: Task 24 사용자 소속별 직위 추가

Task 24 문서:

```text
docs/handoffs/2026-06-08-task-24-user-organization-position.md
```

핵심 내용:

- `user_organizations.position_id`를 추가하는 V5 Flyway 마이그레이션을 작성했다.
- `user_organizations`는 소속별 조직/직위의 원본이고, `users.organization_id`, `users.position_id`는 대표 소속의 mirror로 유지한다.
- 관리자 사용자 API와 UI의 `organizationMemberships`에 `positionId`, `positionName`을 추가했다.
- 결재선 resolver의 `ORG_POSITION` 결재자 조회 기준을 `users.position_id`에서 `user_organizations.position_id`로 변경했다.
- 관리자 사용자 화면은 각 소속 행에서 `소속 조직`과 `소속 직위`를 함께 편집한다.
- 사용자 목록 조직 요약은 `조직 / 직위`, `겸직 : 조직 / 직위(활성|비활성)` 형식으로 줄바꿈 표시한다.
- 신청서 작성 화면의 결재 기준 조직 선택지에서는 대표 소속의 `(대표)` suffix를 제거했다.
- 예상 결재선 API/UI는 결재자별 `조직명 이름 직위명`을 표시하고, 단계 badge 크기를 줄였다.
- 신청자와 결재자가 같은 단계는 결재선 스냅샷을 유지한 채 자동 승인하고, 예상 결재선에는 짧은 `자동` badge만 표시한다.
- 현재 작업은 아직 커밋하지 않았으며, 신규 마이그레이션 파일 `backend/src/main/resources/db/migration/V5__add_position_to_user_organizations.sql`이 commit 범위에서 빠지면 안 된다.

검증 완료:

```bash
docker compose up -d postgres-test
docker run --rm --network host -v /Users/kyh/theieum:/workspace -w /workspace/backend eclipse-temurin:21 ./gradlew test --tests com.theieum.approval.user.UserOrganizationServiceTest --tests com.theieum.approval.approval.ApprovalLineResolverTest --tests com.theieum.approval.common.DatabaseMigrationTest --tests com.theieum.approval.admin.AdminHardDeleteServiceTest
docker run --rm --network host -v /Users/kyh/theieum:/workspace -w /workspace/backend eclipse-temurin:21 ./gradlew test --tests com.theieum.approval.application.ApplicationSubmissionTest --tests com.theieum.approval.application.ApprovalActionTest
docker run --rm --network host -v /Users/kyh/theieum:/workspace -w /workspace/backend eclipse-temurin:21 ./gradlew test
cd frontend && npm run test -- AdminReferencePage.test.tsx
cd frontend && npm run test -- ApplicationForm.test.tsx
cd frontend && npm run test
cd frontend && npm run build
git diff --check
docker compose config
docker compose --env-file .env.staging.example -f docker-compose.staging.yml config
docker compose up --build -d postgres backend frontend
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000
```

실제 결과:

- backend 집중 테스트 통과
- backend 자동승인 targeted 테스트 통과
- backend 전체 테스트 112개 통과
- frontend `AdminReferencePage.test.tsx` 14개 통과
- frontend `ApplicationForm.test.tsx` 10개 통과
- frontend 전체 테스트 69개 통과
- frontend build 통과
- `git diff --check` 통과
- local/staging compose config 통과
- Docker backend/frontend/postgres 실행 중
- Flyway v5 적용 확인
- `http://localhost:3000` HTTP 200 확인
- 실제 API로 `기획팀` 기준 예상 결재선에 조직/직위명과 `autoApprovalExpected`가 포함되는 것 확인

## 최신 Git 정리 상태: 2026-06-05, 문서 갱신: 2026-06-08

아래 Git 정리 상태는 2026-06-05 merge cleanup 당시 기록이다. 현재 작업 브랜치는 상단의 `현재 브랜치` 값을 기준으로 본다.

- Task 15~17 작업은 PR #3으로 `main`에 merge되었다.
- Task 18~22 전 안정화/첨부/캘린더 UX 작업은 `main`에 직접 커밋 후 `origin/main`에 푸시되었다.
- 로컬 `main`은 `origin/main`을 추적하도록 설정되었다.
- 로컬 `HEAD`, `origin/main`, `origin/HEAD`는 같은 커밋을 가리킨다.
- 원격 기본 브랜치는 `main`으로 확인했다.
- 원격에는 `origin/main`만 남아 있다.
- 로컬 작업 브랜치는 `main`만 남아 있다.
- 삭제 완료 브랜치:
  - `codex/receipt-approval-review-followups`
  - `codex/task-15-mobile-pwa-staging`
- 현재 최신 커밋:

```text
ef2de84 feat: harden staging attachments and calendar ux
```

## 최신 업데이트: Task 23 다중 소속/겸직 결재 기준 조직 구현/검증

Task 23 문서:

```text
docs/handoffs/2026-06-08-task-23-multi-organization-approval.md
docs/admin-user-guide.md
docs/staging-trial-data-and-attachments.md
```

핵심 내용:

- Task 23 계획서 기준으로 다중 소속/겸직 결재 기준 조직 기능을 백엔드, 프론트엔드, 문서까지 구현/검증했다.
- V4 마이그레이션으로 `user_organizations`와 `applications.approval_organization_id`를 추가하고 기존 사용자/신청서 데이터를 backfill했다.
- `UserOrganizationService`를 추가하고, 관리자 사용자 API가 복수 소속 목록을 조회/저장하도록 확장했다.
- 사용자 소속 원본은 `user_organizations`이며, `users.organization_id`는 기존 화면/인증/API 호환을 위한 대표 소속 mirror로 유지한다.
- 관리자 사용자 관리에서는 소속 목록을 편집하고 대표 소속을 1개만 지정한다. 대표 소속은 활성 소속이어야 하며, 저장 시 `user_organizations.primary_flag`와 `users.organization_id` mirror가 함께 동기화된다.
- 신청서 작성/수정 API와 UI에서는 `결재 기준 조직`을 저장/선택한다. 선택지는 본인의 활성 소속으로 제한되고 대표 소속이 기본 선택된다.
- 예상 결재선과 제출 시 생성되는 결재선은 모두 같은 선택 조직 기준으로 산정된다.
- 제출 API는 별도 조직 ID를 받지 않고, 임시저장/수정에 저장된 `approvalOrganizationId`를 사용한다.
- 신청서 상세 화면에는 저장된 결재 기준 조직을 표시한다.
- 결재선 resolver는 선택 조직의 활성 멤버십 기준으로 `ORG_POSITION` 결재자를 찾고, 동일 결재자 중복 step을 제거한다.
- 조직/사용자 hard delete는 `user_organizations`와 `applications.approval_organization_id` 참조를 함께 보호한다.
- seed 조직 트리에 `더이음사랑의교회` 아래 예배부/총무부/재정부/미래준비부와 팀들이 추가되었다.
- 아래 Task 22 블록의 “별도 Task 23 없음” 문구는 Task 22 완료 당시 상태를 기록한 과거 문구이며, 이번 최신 업데이트가 이후 상태다.

문서 반영 파일:

```text
backend/src/main/resources/db/migration/V4__add_user_organizations_and_approval_organization.sql
backend/src/main/java/com/theieum/approval/user/UserOrganizationService.java
backend/src/main/java/com/theieum/approval/admin/AdminController.java
backend/src/main/java/com/theieum/approval/application/ApplicationController.java
backend/src/main/java/com/theieum/approval/application/ApplicationService.java
backend/src/main/java/com/theieum/approval/approval/ApprovalLineResolver.java
frontend/src/admin/AdminReferencePage.tsx
frontend/src/applications/ApplicationForm.tsx
frontend/src/applications/ApplicationDetailPage.tsx
docs/admin-user-guide.md
docs/staging-trial-data-and-attachments.md
docs/handoff-2026-06-03.md
docs/handoffs/2026-06-08-task-23-multi-organization-approval.md
```

검증 완료:

```bash
cd frontend && npm run test
cd frontend && npm run build
cd backend && TMPDIR=/private/tmp GRADLE_USER_HOME=/Users/kyh/theieum/.gradle-home JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./gradlew test
docker compose config
docker compose --env-file .env.staging.example -f docker-compose.staging.yml config
git diff --check
```

실제 결과:

- `cd frontend && npm run test`: 69개 테스트 통과
- `cd frontend && npm run build`: TypeScript check와 Vite production build 통과
- `cd backend && ... ./gradlew test`: 108개 테스트 통과
- `docker compose config`: 통과
- `docker compose --env-file .env.staging.example -f docker-compose.staging.yml config`: 통과
- `git diff --check`: 통과
- `docker compose up --build -d postgres backend frontend`: 최신 이미지 빌드 후 `localhost:3000` frontend, `localhost:8080` backend, PostgreSQL healthy 상태로 기동 확인
- 인앱 브라우저에서 `http://localhost:3000/login` 접속 및 `더이음 전자결재` 로그인 화면 표시 확인

남은 확인 사항:

- 실제 스테이징 배포 전에는 `docs/staging-trial-data-and-attachments.md`의 다중 소속/결재 기준 조직 체험 점검을 운영자가 따라 확인한다.
- commit 전에는 현재 작업 트리에 남아 있는 Task 21/22 문서 변경과 `AGENTS.md` 미추적 파일이 이번 commit 범위에 포함될지 별도로 확인한다.

## 이전 업데이트: Task 22 최종 사용자 시나리오 점검 완료

Task 22 최종 점검 문서:

```text
docs/handoffs/2026-06-08-task-22-final-user-scenario-check.md
```

핵심 내용:

- Task 22 스테이징 체험 운영 전 최종 사용자 시나리오 점검을 실제 화면 기준으로 완료했다.
- 신청자 화면에서 다중 영수증 이미지 2개 선택, 작성 화면 확대 미리보기, 제출 후 상세 화면 첨부 표시와 상세 확대 미리보기를 확인했다.
- 관리자 전체 신청서 화면에서 2026-06 월별 첨부 ZIP 다운로드와 다운로드 완료 메시지를 확인했다.
- 다운로드 ZIP 내부에 영수증 일자 기준 2026-06 첨부 entry가 포함되는지 확인했다.
- 모바일 390x844 대시보드에서 일정 칩 클릭과 빈 날짜 영역 클릭 모두 해당 날짜의 세부 일정으로 전환되는지 확인했다.
- 검증 결과 추가 코드 수정은 필요하지 않다.
- Task 22까지 계획된 task는 완료된 상태이며, 현재 문서상 별도 Task 23은 잡혀 있지 않다.
- Docker 이미지 누적 방지를 위해 앞으로 앱 이미지는 안정 이미지명(`theieum-backend`, `theieum-frontend`)을 갱신하는 방식으로 작업하도록 `AGENTS.md`에 규칙을 추가했다.

검증 완료:

```bash
node /private/tmp/task22-visual-scenario.mjs
unzip -l /private/tmp/task22-monthly-download.zip
docker compose -p theieum_task22_verify logs --tail=80 backend
git diff --check
```

결과:

- `node /private/tmp/task22-visual-scenario.mjs`는 `ok: true`로 완료되었다.
- ZIP에는 검증 중 생성된 `application-2`의 `task22-receipt-a.png`, `task22-receipt-b.png`가 포함되었다.
- 백엔드 tail 로그에는 검증 시나리오 중 신규 오류 로그가 보이지 않았다.
- `git diff --check`는 통과했다.

남은 확인 사항:

- 실제 외부 스테이징 URL 배포 후에는 `docs/staging-operations-runbook.md` 기준으로 smoke와 모바일 E2E를 다시 실행한다.
- 추가 요청이 없다면 기능 구현 task는 Task 22에서 종료된 상태로 본다.

## 이전 업데이트: Task 22 전 안정화 커밋 및 모바일 캘린더 클릭 보정

최신 반영 상태:

```text
ef2de84 feat: harden staging attachments and calendar ux
```

핵심 내용:

- Task 18~22 전 작업 묶음이 `main`과 `origin/main`에 반영되었다.
- 모바일 대시보드 캘린더에서 일정 칩 또는 빈 날짜 영역을 눌러도 아래 세부 일정이 해당 날짜로 바뀌도록 보정했다.
- 읽기 전용 일정 칩은 편집/입력이 아니라 날짜 선택용 버튼으로 동작한다.
- 날짜 셀 전체 선택 hitbox와 pointer-event 레이어를 정리해 모바일에서 빈 `calendar-chip-list`가 터치를 가로채지 않게 했다.
- 같은 `CalendarBoard` 컴포넌트를 쓰는 `/dashboard`와 `/calendar`에 동일하게 적용된다.
- Docker frontend 이미지를 재빌드하고 `localhost:3000` frontend 컨테이너를 교체했다.

검증 완료:

```bash
cd frontend && npm run test
cd frontend && npm run build
docker run --rm -v /Users/kyh/theieum:/workspace -w /workspace/backend eclipse-temurin:21 ./gradlew test
git diff --check
docker compose build frontend
docker compose up -d frontend
```

결과:

- 프론트 전체 테스트 66개 통과
- 프론트 타입 체크와 production build 통과
- 백엔드 전체 테스트는 호스트 Java 21 toolchain 부재로 Docker Java 21 컨테이너에서 통과
- 모바일 412x915 Chromium 검증에서 `철야` 일정 칩 클릭과 `2026-06-11` 빈 날짜 영역 클릭 모두 세부 일정 전환 확인
- `git diff --check` 통과
- `docker compose ps` 기준 backend, frontend, postgres 실행 중이며 postgres healthy

## 이전 업데이트: Task 22 전 다중 영수증 첨부 및 월별 다운로드

Task 22 전 후속 문서:

```text
docs/superpowers/specs/2026-06-05-multi-receipt-attachments-design.md
docs/superpowers/plans/2026-06-05-multi-receipt-attachments.md
docs/handoffs/2026-06-05-task-22-pre-multi-receipt-attachments.md
docs/staging-trial-data-and-attachments.md
```

핵심 내용:

- 신청서당 영수증 이미지 기본 첨부 개수를 최대 10개로 확장
- 신청서 작성 화면에서 다중 파일 선택, 썸네일 그리드, 개별 삭제, 확대 미리보기 제공
- 신청서 상세 화면에서 여러 첨부 썸네일과 확대 미리보기 제공
- 관리자 전체 신청서 화면에 월별 첨부 ZIP 다운로드 UI 추가
- 관리자 전용 `GET /api/admin/attachments/monthly-download?month=YYYY-MM` API 추가
- 월별 ZIP은 영수증 일자 기준으로 묶고, 매니저 접근은 403으로 차단
- 프론트 전체 테스트 66개, backend 전체 테스트, frontend build, staging compose config, `git diff --check` 통과

후속 상태:

```text
Task 22 스테이징 체험 운영 전 최종 사용자 시나리오 점검 완료
```

## 이전 업데이트: Task 21 체험 계정/기준정보 및 첨부파일 관리 안정화

Task 21 문서:

```text
docs/superpowers/specs/2026-06-05-trial-accounts-attachments-design.md
docs/superpowers/plans/2026-06-05-trial-accounts-attachments-stability.md
docs/staging-trial-data-and-attachments.md
docs/handoffs/2026-06-05-task-21-trial-accounts-attachments.md
```

핵심 내용:

- seed 계정을 30명 안팎 체험 기준으로 확장
- `trial-applicant01` ~ `trial-applicant06`, `trial-approver01`, `trial-approver02`, `trial-manager01`, `trial-admin01` 추가
- `trial-manager01`은 `MANAGER` 역할 포함
- 첨부파일 크기 제한을 `app.attachments.max-image-bytes` / `ATTACHMENT_MAX_IMAGE_BYTES` 설정으로 분리
- 신청서당 첨부 개수 제한을 `app.attachments.max-files-per-application` / `ATTACHMENT_MAX_FILES_PER_APPLICATION` 설정으로 추가
- 기본 첨부 정책은 신청서당 영수증 이미지 최대 10개, 파일당 5MB 이하로 확장
- 스테이징 계정 풀, 기준정보 점검, 첨부파일 보존/삭제 절차를 `docs/staging-trial-data-and-attachments.md`에 정리
- Java 21 Docker 컨테이너에서 backend 전체 테스트 89개 통과
- staging compose 정적 렌더링 검증 통과

후속 상태:

```text
Task 22 스테이징 체험 운영 전 최종 사용자 시나리오 점검 완료
```

## 이전 업데이트: Task 20 결재 프로세스 안정화

Task 20 문서:

```text
docs/superpowers/specs/2026-06-05-process-stability-design.md
docs/superpowers/plans/2026-06-05-process-stability-hardening.md
docs/handoffs/2026-06-05-task-20-process-stability.md
```

핵심 내용:

- 결재 신청, 승인/반려, 권한, 알림, 삭제 제한의 백엔드 회귀 테스트 보강
- 작성자가 아닌 사용자의 제출 차단 검증
- 취소 신청서는 재수정 전 제출 불가 검증
- 미래 결재자는 차례 전 상세/첨부 접근과 승인 처리 차단 검증
- 차례가 온 결재자는 상세/첨부 접근 가능 검증
- 승인완료/반려 신청서는 신청자와 관리자 API 모두 완전 삭제 차단 검증
- 알림 읽음 처리는 수신자만 가능함을 검증
- 프로덕션 코드 변경 없음
- Java 21 Docker 컨테이너에서 backend 전체 테스트 88개 통과

## 이전 업데이트: Task 19 Oracle Always Free 스테이징 배포 준비

재시작 트리거:

```text
Oracle Always Free 기반 스테이징 서버 배포를 진행해주세요.
```

위 요청이 들어오면 Task 19를 다시 열고, 실제 Oracle VM 생성 전 필요한 입력값을 확인한 뒤 진행한다.

Task 19 문서:

```text
docs/oracle-free-staging-runbook.md
docs/superpowers/plans/2026-06-05-oracle-free-staging-deployment.md
docs/handoffs/2026-06-05-task-19-oracle-free-staging.md
```

핵심 내용:

- Oracle Cloud Always Free VM 기반 외부 체험 서버 배포 경로 정리
- 30명 안팎 체험 기준으로 Ampere A1 2 OCPU / 12GB RAM부터 시작 권장
- frontend는 `127.0.0.1:3000`에 바인딩하고 외부는 Caddy `80/443`만 공개
- `3000`, `8080`, `5432` 외부 직접 공개 금지
- GitHub public 유지 조건과 private 전환/read-only deploy key 경로 정리
- 실제 VM 생성은 Oracle 계정, region, SSH 공개키, staging domain 결정 후 진행

## 이전 업데이트: Task 18 스테이징 외부 체험 운영 준비

Task 18 문서:

```text
docs/superpowers/plans/2026-06-05-staging-pilot-readiness.md
docs/handoffs/2026-06-05-task-18-staging-pilot-readiness.md
docs/staging-operations-runbook.md
docs/staging-tester-account-packet.md
```

핵심 내용:

- 외부 체험 운영자용 스테이징 런북 추가
- 체험자별 계정 전달 양식 추가
- 개인별 계정과 공유 확인용 계정의 비밀번호 변경 안내 분리
- 매니저 체험은 전달 전 `MANAGER` 권한 계정 확인 필요
- `scripts/staging-smoke.sh`로 frontend, PWA manifest, API proxy smoke 확인
- Playwright E2E base URL을 `E2E_BASE_URL`로 원격 staging URL에 재사용 가능
- 로컬 Docker 앱에서 smoke와 모바일 PWA E2E 검증 완료

## 이전 업데이트: Task 17 공용 캘린더

Task 17 문서:

```text
docs/superpowers/specs/2026-06-04-shared-calendar-design.md
docs/superpowers/plans/2026-06-04-shared-calendar-implementation.md
docs/handoffs/2026-06-04-task-17-shared-calendar.md
```

핵심 내용:

- 내부 공용 캘린더 도메인 `calendar_events` 추가
- 모든 로그인 사용자는 대시보드와 `/calendar`에서 일정 조회 가능
- `ADMIN`과 `MANAGER`만 일정 생성, 수정, 삭제 가능
- 대시보드와 `/calendar` 모두 월/주/목록 달력형 보기 제공
- `/calendar`에 날짜 셀 일정 칩, 날짜별 빠른 추가 버튼, 날짜/시간 분리 입력 모달 추가
- 주 보기 이전/다음은 7일 단위로 이동
- 시간 일정과 종일 일정의 from~to 표시 규칙 정리
- 사이드바와 모바일 하단 탭에 `캘린더` 메뉴 추가
- Google Calendar 연동은 Task 17 범위에서 제외하고 후속 task로 분리

## 이전 업데이트: Task 16 데이터 완전 삭제

Task 16 문서:

```text
docs/superpowers/specs/2026-06-04-hard-delete-policy-design.md
docs/superpowers/plans/2026-06-04-hard-delete-policy-implementation.md
docs/handoffs/2026-06-04-task-16-hard-delete-policy.md
```

핵심 내용:

- 기존 기준정보 삭제 동작은 `비활성화`로 유지
- 사용자, 조직, 직위, 결재선, 조직별 예외 결재자 `ADMIN` 완전 삭제 추가
- 신청서는 `DRAFT`/`CANCELED`만 작성자 본인 또는 `ADMIN`이 완전 삭제 가능
- `IN_APPROVAL`, `APPROVED`, `REJECTED` 신청서는 업무 기록 보호를 위해 완전 삭제 차단
- 신청서 완전 삭제 시 첨부 DB row와 실제 파일 삭제
- 관리 UI에 복구 불가 확인 모달과 서버 차단 메시지 추가

## 이전 업데이트: Task 15 모바일/PWA 사용성 개선과 외부 체험 스테이징

Task 15 문서:

```text
docs/superpowers/specs/2026-06-04-mobile-pwa-staging-design.md
docs/superpowers/plans/2026-06-04-mobile-pwa-staging-implementation.md
docs/handoffs/2026-06-04-task-15-mobile-pwa-staging.md
```

핵심 내용:

- 외부 체험 기본 방식은 작은 스테이징 서버
- 로컬 MVP compose와 staging compose 분리
- 테스트 계정/체험자 가이드 추가
- PWA manifest와 모바일 홈 화면 메타 추가
- 모바일 하단 탭, 신청서 작성, 결재함, 상세 화면 사용성 개선
- 관리자 기준정보 화면 모바일 깨짐 완화

## 1. 현재 상태 요약

현재 코드 기준 최신 커밋은 다음이다.

```text
ef2de84 feat: harden staging attachments and calendar ux
```

이 커밋까지 아래 작업은 완료되어 `main`에 반영되어 있다.

- Spring Boot REST API, React/Vite, PostgreSQL, Docker Compose 기반 MVP
- 로그인, JWT 인증, 역할 기반 라우팅과 API 권한
- 사용자, 조직, 직위, 결재선, 조직별 예외 결재자 기준정보
- 영수증 첨부 신청서 작성, 임시저장, 제출, 취소, 재수정
- 결재함, 승인, 반려, 관리자/매니저 예외 결재
- 신청서 상세, 첨부 이미지 미리보기, 결재 진행 상태, 결재 이력
- 인앱 알림함, 관리자 전체 신청서, 관리자 알림 로그
- `ADMIN`/`MANAGER` 관리화면 권한과 기준정보 CRUD
- 결재선 관리 UI 개선
  - 결재유형 콤보
  - 사용자/조직/직위 콤보
  - 직접 사용자 `#ID 이름` 표시
  - 조직범위 `APPLICANT_ORG`, `PARENT_ORG`, `ROOT_ORG`
  - `approvalTypeName` 응답 표시
- 신청서 작성 화면 예상 결재선 표시
- 필수값 표시, 로그인 사용자 이름 표시, 로그아웃 확인창
- Task 15 모바일/PWA 사용성 개선과 외부 체험 스테이징 구성
- Task 16 사용자/조직/직위/결재선/예외결재자/일부 신청서 완전 삭제
- Task 17 내부 공용 캘린더와 대시보드 달력형 보기
- Task 18 스테이징 외부 체험 운영 런북과 검증 절차
- Task 19 Oracle Always Free 스테이징 서버 배포 준비 문서와 재시작 트리거
- Task 20 결재 프로세스 안정화 회귀 테스트 보강
- Task 21 체험 계정/기준정보 및 첨부파일 관리 안정화
- Task 22 전 다중 영수증 첨부, 월별 첨부 ZIP 다운로드, 모바일 캘린더 클릭 보정
- Task 22 스테이징 체험 운영 전 최종 사용자 시나리오 점검

2026-06-05 Task 18~22 전 작업은 `ef2de84`로 커밋되어 `origin/main`에 푸시되었다.

## 2. Task 15 구현 범위

Task 15 목표:

- 외부 테스트 인원 5~10명이 안정적으로 체험할 수 있는 스테이징 구성을 준비한다.
- 모바일 브라우저와 PWA 설치 환경에서 앱처럼 편하게 사용할 수 있도록 핵심 UX를 개선한다.

먼저 읽을 문서:

```text
docs/superpowers/specs/2026-06-04-mobile-pwa-staging-design.md
docs/superpowers/plans/2026-06-04-mobile-pwa-staging-implementation.md
```

Task 15 완료 구현 단위:

1. 단독 `docker-compose.staging.yml`과 `.env.staging.example` 추가
2. 외부 체험자 가이드와 배포 문서 보강
3. PWA manifest, 아이콘, HTML 메타 추가
4. 모바일 하단 탭과 더보기 관리 메뉴 추가
5. 신청서 작성 모바일 첨부/하단 액션 UX 개선
6. 결재함 모바일 카드형 목록 추가
7. 신청서 상세 모바일 액션/이력 카드 추가
8. 관리자 기준정보 화면 모바일 깨짐 완화
9. 모바일/PWA Playwright smoke 추가
10. Task 15 완료 후 새 인수인계서 작성

## 3. 새 채팅 재시작 절차

새 채팅에서 이어갈 때는 아래 순서로 확인한다.

```bash
cd /Users/kyh/theieum
git status --short
git branch -vv
git branch -r -vv
git log --oneline -5
```

Docker 상태도 확인한다.

```bash
docker ps -a --format '{{.Names}}\t{{.Status}}'
```

Docker 검증이 필요한 작업은 Docker가 켜진 뒤 현재 컨테이너 상태와 로그를 다시 확인한다.

## 4. 검증 기준

일반 프론트 검증:

```bash
cd /Users/kyh/theieum/frontend
npm run test
npm run build
```

일반 compose 정적 검증:

```bash
cd /Users/kyh/theieum
docker compose config
```

Task 15 스테이징 compose 정적 검증:

```bash
cd /Users/kyh/theieum
docker compose --env-file .env.staging.example -f docker-compose.staging.yml config
```

정리/문서 변경 검증:

```bash
git diff --check
```

백엔드 전체 테스트는 로컬 Java 21 toolchain 상태에 따라 실패할 수 있다. 호스트에서 Java 21을 사용할 수 없으면 기존에 검증된 방식처럼 Java 21 Docker 컨테이너를 사용한다.

Task 17 백엔드 검증은 Docker JDK 21 컨테이너에서 `./gradlew test`로 수행했다. Task 17 작업 중 호스트 Gradle은 Java 21 toolchain 부재로 실패했다.

## 5. 운영/스테이징 주의점

현재 `docker-compose.yml`은 로컬 MVP 검증용이다. 외부 체험이나 운영 후보 환경에 그대로 공개하지 않는다.

주의할 값:

- `SPRING_PROFILES_ACTIVE`
- `JWT_SECRET`
- DB 계정과 비밀번호
- `FILE_STORAGE_ROOT`
- seed/test 데이터 적용 여부
- PostgreSQL 외부 포트 공개 여부
- backend 직접 외부 공개 여부
- HTTPS 적용 여부

외부 체험 기본 방향은 작은 스테이징 서버에 단독 staging compose를 올리는 것이다. Cloudflare Tunnel 또는 ngrok은 아주 빠른 임시 공유용 보조안으로만 사용한다.

## 6. 계정과 권한 메모

seed/local profile 기준 기본 비밀번호는 `password`다.

주요 seed 계정:

- `admin/password`: 관리자
- `employee01/password`: 기안자
- `lead-dev/password`: 개발팀 결재자
- `approver01/password`: 기본 결재선 검증용 결재자
- `ceo/password`: seed SQL 기준 `APPROVER` 역할 계정이다. 매니저 체험은 전달 전 `MANAGER` 역할이 있는 계정을 별도로 확인한다.

권한 정책:

- 일반 사용자는 관리 메뉴를 볼 수 없고 `/admin/...` 직접 접근 시 `/dashboard`로 이동한다.
- `ADMIN`과 `MANAGER`는 관리 메뉴에 접근할 수 있다.
- 사용자 관리의 민감 CRUD와 타 사용자 비밀번호 변경은 `ADMIN` 중심으로 유지한다.
- 대리결재는 제외하고, 관리자/매니저 예외 결재를 사용한다.
- 관리자 예외 결재 사유는 필수 입력 경로다.

## 7. 정리된 인수인계서

아래 task별 인수인계서는 최신 상태와 맞지 않거나 루트 문서와 설계/계획 문서로 대체 가능하여 제거했다.

```text
docs/handoffs/2026-06-03-task-11-docker-execution-harness.md
docs/handoffs/2026-06-03-task-12-playwright-e2e-flow.md
docs/handoffs/2026-06-03-task-13-admin-manager-crud.md
docs/handoffs/2026-06-03-task-14-approval-line-usability.md
```

향후에는 작업 완료 시점에만 새 task별 인수인계서를 만든다. 계획 단계 문서는 `docs/superpowers/specs/`와 `docs/superpowers/plans/`를 사용하고, 실제 구현 완료 후 필요한 경우 `docs/handoffs/YYYY-MM-DD-task-XX-...md`를 새로 만든다.
